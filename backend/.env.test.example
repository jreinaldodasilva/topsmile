# Test Environment Variables
NODE_ENV=test

# Database
DATABASE_URL=mongodb://localhost:27017/topsmile_test

# Redis (optional for tests - mocked by default)
REDIS_URL=redis://localhost:6379

# JWT Secrets (generate with: openssl rand -hex 32)
TEST_JWT_SECRET=your-64-character-jwt-secret-for-tests
TEST_PATIENT_JWT_SECRET=your-64-character-patient-jwt-secret-for-tests

# Test Passwords (use strong passwords)
TEST_DEFAULT_PASSWORD=SecureTestPass123!
TEST_PATIENT_PASSWORD=PatientTestPass123!
TEST_ADMIN_PASSWORD=AdminTestPass123!

# Email (mocked by default)
SENDGRID_API_KEY=test-key-not-used-in-tests